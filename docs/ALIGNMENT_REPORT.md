# LabelHub 需求对齐报告

> 生成时间：2025-12-13  
> 目的：对照新增/明确的需求，识别 PRD 与 MILESTONES 中的差距，指导修订

---

## 1. 已满足的硬需求

| 硬需求 | PRD 章节 | 状态 | 备注 |
|--------|----------|------|------|
| 任务类型：分类 | §3.1 | ✅ 满足 | MVP 必做 |
| 任务类型：目标检测 (bbox) | §3.1 | ✅ 满足 | MVP 必做 |
| 任务类型：分割 (polygon) | §3.1 | ✅ 满足 | MVP 必做；Mask 明确为 v1.1 |
| 快捷键系统 | §3.2.3 | ✅ 满足 | 键位定义完整 |
| Undo/Redo | §3.2.4 | ✅ 满足 | 50 步历史栈 |
| Skip/Delete | §2.2 A-07 | ✅ 满足 | 跳过必填原因 |
| 标注进度显示 | §3.2.5 | ✅ 满足 | 进度条 + Dashboard |
| 标注统计 | §6 | ✅ 满足 | KPI 定义完整 |
| 后端 FastAPI | §8.2 | ✅ 满足 | 技术选型已锁定 |
| 前端无感加载思路 | §5.1 | ✅ 满足 | 缩略图/缓存/预取/虚拟列表方向正确 |
| 现代 UI / 骨架屏 / 键位提示 | §4, §5.2 | ✅ 满足 | 页面设计已有 |

---

## 2. 未满足 / 口径不一致的点

### 2.1 🔴 预标注导入：缺失 Parser Template 系统

| 问题点 | 当前 PRD 口径 | 应有口径 | 影响 |
|--------|--------------|----------|------|
| **格式支持** | 写死 COCO/YOLO/VOC 三种解析器 | 任意 JSON/JSONL + 声明式映射模板 | 后端需要写死解析器 vs 通用模板引擎，架构差异大 |
| **映射语言** | 无 | JMESPath (首选) / JSONPath (可选兼容) | 决定依赖选型与安全边界 |
| **JSONL 流式** | 无 | 必须支持流式解析，不一次性读入内存 | 大文件 OOM 风险 |
| **模板管理** | 无 | 模板 CRUD、内置模板、用户自定义 | 需要新增数据模型 `ParserTemplate` |
| **测试预览** | 10 张样例 | 前 20 条 + 错误定位到行号/索引 | 体验差异，错误需精确定位 |
| **安全限制** | 无 | 表达式沙箱、超时、深度限制 | 安全风险 |

**需修改章节：** PRD §3.2.1 预标注导入 (完全重写)  
**需修改里程碑：** M2 预标注导入部分 (改为 Parser Template 系统)

---

### 2.2 🔴 慢网缓存策略：缺少可执行口径

| 问题点 | 当前 PRD 口径 | 应有口径 | 影响 |
|--------|--------------|----------|------|
| **缩略图缓存** | 仅提"CDN/Nginx 缓存" | 具体 Cache-Control 值、ETag 策略 | 实现时口径不一 |
| **原图缓存** | 无 | 区分缩略图/中图/原图的缓存策略 | 带宽与存储平衡 |
| **乐观锁冲突** | 提到 version 字段 | If-Match 头、412/409 选择、错误体结构 | API 设计不统一 |
| **大文件断点** | 无 | Range/206 支持 | 大图加载中断后需重新下载 |
| **Service Worker** | 仅提"缓存静态资源" | 具体策略：stale-while-revalidate / cache-first | 前端实现口径不一 |

**需修改章节：** PRD §5.1 性能需求 (补充缓存口径小节)

---

### 2.3 🟡 数据集导入口径：优先级未明确

| 问题点 | 当前 PRD 口径 | 应有口径 | 影响 |
|--------|--------------|----------|------|
| **导入方式** | "文件夹路径/ZIP上传" 并列 | 服务器路径索引/对象存储引用为优先；ZIP 上传为可选(MVP 可推迟) | 实现顺序与工作量 |

**需修改章节：** PRD §2.2 M-02、§3.2.1

---

### 2.4 🟡 状态机不完整

| 问题点 | 当前 PRD 口径 | 应有口径 | 影响 |
|--------|--------------|----------|------|
| **状态枚举** | pending/in_progress/completed/skipped | 增加 `deleted` (软删除) | 数据模型字段 |
| **状态命名** | pending | 建议统一为 `todo` (与通用术语一致) | 代码可读性 |

**需修改章节：** PRD §7.2 状态机

---

### 2.5 🟡 技术选型模糊：UI 组件库

| 问题点 | 当前 PRD 口径 | 应有口径 | 影响 |
|--------|--------------|----------|------|
| **UI 组件** | "Ant Design / Radix UI" 二选一未定 | 明确选择一套 | 团队学习成本、样式一致性 |
| **画布库** | "Fabric.js / Konva" 二选一未定 | 明确选择 Fabric.js，并说明与 Undo/Redo 的集成方式 | 架构设计 |

**需修改章节：** PRD §8.2 技术选型

---

### 2.6 🟡 里程碑格式误导

| 问题点 | 当前格式 | 应有格式 | 影响 |
|--------|----------|----------|------|
| **交付项勾选** | `[x]` 已勾选 | `[ ]` 或纯列表 | 误导为已完成 |

**需修改：** MILESTONES.md 全文 `[x]` → `[ ]` 或去掉 checkbox

---

### 2.7 🟡 统计数据源口径缺失

| 问题点 | 当前口径 | 应有口径 | 影响 |
|--------|----------|----------|------|
| **数据源** | 直接查业务表 | 明确"以事件日志为数据源" | 统计准确性、可追溯性 |

**需修改：** PRD §6 / MILESTONES M3

---

## 3. 修改位置与改法

### PRD.md 修改清单

| 章节 | 修改类型 | 具体改法 |
|------|----------|----------|
| §3.2.1 预标注导入 | **重写** | 改为 Parser Template 系统：输入/模板/输出/体验/校验/安全/内置模板 |
| §5.1 性能需求 | **补充** | 新增 §5.1.1 HTTP 缓存口径：缩略图/中图/原图的 Cache-Control、ETag、304；乐观锁 If-Match；Range/206；SW 策略 |
| §2.2 M-02 | **修订** | 数据导入优先级：服务器路径 > 对象存储 > ZIP 上传(可选) |
| §7.2 状态机 | **修订** | 状态改为 todo/in_progress/done/skipped/deleted |
| §8.2 技术选型 | **修订** | UI 明确选 Tailwind + shadcn/ui；画布明确 Fabric.js + Command pattern |
| §6 效率 KPI | **补充** | 注明"统计以事件日志为数据源" |
| §9.1 API 设计 | **补充** | 新增 Parser Template CRUD 端点 |

### MILESTONES.md 修改清单

| 章节 | 修改类型 | 具体改法 |
|------|----------|----------|
| 全文 | **格式** | `[x]` → `[ ]` 或改为无 checkbox 列表 |
| M2 预标注导入 | **重写** | 改为 Parser Template 系统落地：后端 API、前端三栏界面、内置模板 |
| M2 验收标准 | **修订** | COCO/YOLO/VOC 改为"内置模板可用 + 自定义模板可用" |
| M3 统计 | **补充** | 注明"统计以事件日志为数据源"口径 |
| 版本对照表 | **修订** | 预标注导入改为 Parser Template |

---

## 4. 返工风险分析

### 4.1 高风险：若不修订将导致返工

| 风险点 | 影响范围 | 返工成本 |
|--------|----------|----------|
| 预标注写死三格式 vs Parser Template | 后端解析模块、前端导入页、数据模型 | **高**：需要重构整个导入流程 |
| 缓存口径不一致 | 后端 API 响应头、前端 SW、Nginx 配置 | **中**：需统一后修改多处 |
| 乐观锁 API 设计不一致 | API 错误处理、前端冲突提示 | **中**：需统一错误码与结构 |

### 4.2 中风险：口径不一导致体验问题

| 风险点 | 影响范围 | 后果 |
|--------|----------|------|
| UI 组件库选型不定 | 前端所有页面 | 样式不一致、依赖冗余 |
| 状态机不完整 | 数据模型、状态流转 | 软删除逻辑混乱 |

### 4.3 低风险：可后补

| 风险点 | 影响范围 | 备注 |
|--------|----------|------|
| 统计数据源口径 | M3 实现时确定 | 可在 M3 前锁定 |

---

## 5. 验收：Parser Template 示例

### 5.1 输入示例 (JSONL，每行一个 record)

```jsonl
{"file": "img_001.jpg", "objects": [{"cls": "cat", "box": [100, 150, 200, 250], "conf": 0.92}]}
{"file": "img_002.jpg", "objects": [{"cls": "dog", "box": [50, 60, 180, 220], "conf": 0.87}, {"cls": "cat", "box": [300, 100, 400, 200], "conf": 0.78}]}
```

### 5.2 Parser Template 定义 (JMESPath)

```yaml
name: "custom_detection_v1"
description: "自定义检测格式模板"
input_type: "jsonl"           # json | jsonl
record_path: null             # JSONL 时为 null；JSON 时可为 "data.images[*]"

mapping:
  image_key: "file"           # 图片文件名/路径字段
  annotations_path: "objects" # 标注数组路径

  annotation:
    label: "cls"              # 类别字段
    score: "conf"             # 置信度字段 (可选)
    
    # 根据标注类型选择一种
    bbox:                     # 目标检测
      format: "xyxy"          # xyxy | xywh | cxcywh (归一化自动检测)
      path: "box"             # 坐标数组路径
    
    # polygon:                # 分割 (二选一)
    #   path: "segmentation"
    #   format: "flat"        # flat: [x1,y1,x2,y2,...] | nested: [[x1,y1],[x2,y2],...]
    
    # classification:         # 分类 (若为图片级分类)
    #   path: "category"

validation:
  required_fields: ["file", "objects"]
  score_range: [0.0, 1.0]
```

### 5.3 映射输出：内部 Prediction 格式

```json
{
  "image_key": "img_001.jpg",
  "predictions": [
    {
      "type": "bbox",
      "label": "cat",
      "score": 0.92,
      "data": {
        "x": 100,
        "y": 150,
        "width": 100,
        "height": 100
      }
    }
  ]
}
```

### 5.4 内置模板：COCO 示例

```yaml
name: "builtin_coco"
description: "COCO 格式 (instances)"
input_type: "json"
record_path: "images[*]"      # 遍历 images 数组

# 需要关联 annotations 数组，使用 JMESPath 的 join
lookup:
  annotations_by_image: "annotations[?image_id==`{image_id}`]"
  categories_map: "categories[*].{id: id, name: name}"

mapping:
  image_key: "file_name"
  annotations_path: "$annotations_by_image"  # 引用 lookup
  
  annotation:
    label: "$categories_map[?id==`{category_id}`].name | [0]"
    score: "score"
    bbox:
      format: "xywh"
      path: "bbox"
    polygon:
      path: "segmentation[0]"
      format: "flat"
```

---

## 6. MVP 功能优先级确认

### P0 必做 (v1.0 MVP)

- [x] 分类/检测/分割 (Polygon) 标注
- [x] Parser Template 系统 (替代写死格式)
- [x] JMESPath 表达式引擎
- [x] COCO/YOLO/VOC 内置模板
- [x] 慢网优化：缩略图/预取/虚拟列表/骨架屏
- [x] HTTP 缓存：ETag/304/Cache-Control
- [x] 乐观锁：If-Match / 412
- [x] 快捷键 + Undo/Redo
- [x] 效率统计 Dashboard
- [x] 服务器路径索引导入数据集

### P1 降级/推迟 (v1.0 可选)

- [ ] ZIP 文件上传导入 (可推迟到 v1.1)
- [ ] Range/206 大文件断点 (可推迟)
- [ ] JSONPath (RFC 9535) 兼容 (JMESPath 优先)
- [ ] 亮度/对比度调整 (可推迟)
- [ ] 深色模式 (可推迟)

### v1.1 明确推迟

- [ ] Mask 画笔分割
- [ ] 离线标注支持
- [ ] 自定义属性字段
- [ ] 标注工具插件机制

### v2 明确推迟

- [ ] 审阅工作流
- [ ] 多人实时协作
- [ ] AI 辅助 (SAM)
- [ ] Webhook / SSO

---

## 7. 下一步行动

1. ✏️ **修订 PRD.md** - 按第 3 节修改清单执行
2. ✏️ **修订 MILESTONES.md** - 按第 3 节修改清单执行
3. 🔒 **团队评审** - 确认 Parser Template 设计满足需求
4. 🚀 **开始 M0** - 基于修订后的文档启动开发
